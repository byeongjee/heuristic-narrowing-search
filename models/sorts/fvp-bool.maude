
set include BOOL off . 

fmod FVP-BOOL is
    sort Boolean .
    ops true false : -> Boolean [ctor] .
    op _and_ : Boolean Boolean -> Boolean [assoc comm] .

    var B : Boolean .
    eq true and B = B [variant] .
    eq false and B = false [variant] .
    eq B and B = B [variant] .
endfm
