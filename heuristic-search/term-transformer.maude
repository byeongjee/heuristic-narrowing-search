
mod TERM-TRANSFORMER is
    protecting META-LEVEL .

    op trTerm : Module Term -> Term .
endm

mod DEFAULT-TERM-TRANSFORMER is
    extending TERM-TRANSFORMER .

    var M : Module . var T : Term .

    eq trTerm(M, T) = T .
endm